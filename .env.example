# Keep this file as a template only. Do not commit a real key.
# The web console also reads %USERPROFILE%\.testpilot\web.env
OPENAI_API_KEY=
OPENAI_MODEL=deepseek-chat
# Optional OpenAI-compatible endpoint:
OPENAI_BASE_URL=https://api.deepseek.com
